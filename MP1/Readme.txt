This mp was written using Microsoft Visual Studio.  To run it, load the solution file into visual studio and build it normally.

Wireframe mode and filled mode, tap the 'w' key while the program is running to switch between the two.
Outline mode, tap the 'o' key to turn on or off the outline